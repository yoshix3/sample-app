import { createStore } from 'vuex'
import counter from './modules/counter'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    counter,
  }
})
